<%- include('../partials/header') %>
<% locals.title = 'Mark Attendance' %>

<div class="page-container">
  <section class="page-title">
    <div>
      <h1>Mark Attendance</h1>
      <p>Meeting: <%= formatDate(meeting.meeting_date) %> (<%= meeting.meeting_type %>)</p>
    </div>
  </section>

  <section class="panel">
    <form method="post" action="/secretary/meetings/<%= meeting.id %>/attendance">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <p style="margin-bottom: var(--space-3); font-size: 0.875rem; color: var(--neutral-600);">
        Mark each member as Present, Late, Permission (Excused), or Absent.
      </p>

      <div class="table-wrap">
        <table class="data-table">
          <thead>
            <tr>
              <th>Member</th>
              <th style="text-align:center">Present</th>
              <th style="text-align:center">Late</th>
              <th style="text-align:center">Permission</th>
              <th style="text-align:center">Absent</th>
            </tr>
          </thead>
          <tbody>
            <% members.forEach(function(m) { %>
            <% const current = attendanceMap[m.id] || 'absent'; %>
            <tr>
              <td><%= m.name %></td>
              <td style="text-align:center">
                <input type="radio" name="member_<%= m.id %>" value="present" <%= current === 'present' ? 'checked' : '' %> aria-label="<%= m.name %> present">
              </td>
              <td style="text-align:center">
                <input type="radio" name="member_<%= m.id %>" value="late" <%= current === 'late' ? 'checked' : '' %> aria-label="<%= m.name %> late">
              </td>
              <td style="text-align:center">
                <input type="radio" name="member_<%= m.id %>" value="excuse" <%= current === 'excuse' ? 'checked' : '' %> aria-label="<%= m.name %> excused">
              </td>
              <td style="text-align:center">
                <input type="radio" name="member_<%= m.id %>" value="absent" <%= current === 'absent' ? 'checked' : '' %> aria-label="<%= m.name %> absent">
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>

      <div class="form-actions" style="margin-top: var(--space-4);">
        <a class="btn btn--secondary" href="/secretary/meetings/<%= meeting.id %>">Cancel</a>
        <button class="btn btn--primary" type="submit">Save Attendance</button>
      </div>
    </form>
  </section>
</div>

<%- include('../partials/footer') %>
